import { Equipment, MuscleGroup, TimeOption } from './types';

export const EQUIPMENT_OPTIONS: Equipment[] = ['Full Gym', 'Dumbbells', 'Bodyweight'];

export const MUSCLE_GROUPS: MuscleGroup[] = [
  'Chest', 'Back', 'Shoulders', 'Arms', 'Legs', 'Core'
];

export const TIME_OPTIONS: TimeOption[] = [15, 30, 45, 60];

export const MOCK_HISTORY = []; // Start empty
